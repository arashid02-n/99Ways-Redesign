# EmDash encryption key (optional but recommended).
# Generate one with:  npx emdash secrets generate
# Copy this file to `.env` and set the value. Never commit `.env`.
EMDASH_ENCRYPTION_KEY=emdash_enc_v1_<paste-generated-key-here>
